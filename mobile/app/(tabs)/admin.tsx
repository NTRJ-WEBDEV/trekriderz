import { View } from 'react-native';
// Placeholder — admin panel is at /admin (app/admin/index.tsx)
// This file exists so Expo Router registers the "admin" tab screen name.
export default function AdminTabPlaceholder() {
  return <View style={{ flex: 1, backgroundColor: '#080C14' }} />;
}
